name: cignal-sky-epg-scraper

on:
  schedule:
    - cron: '0 */8 * * *'  # Every 8 hours
  workflow_dispatch:

jobs:
  fetch_epg:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install requests pytz beautifulsoup4

      - name: Run EPG fetcher (Cignal + SkyCable)
        run: python epg_fetcher.py

      - name: Commit and push if changes
        run: |
          git config --global user.name "actions-user"
          git config --global user.email "actions@github.com"
          git remote set-url origin https://x-access-token:${{ secrets.CIGNAL_EPG }}@github.com/Alobadman/CIGNAL_EPG.git
          git add cignal_epg.xml cignal_epg.xml.gz sky_epg.xml sky_epg.xml.gz
          git diff --staged --quiet || git commit -m "Update Cignal & SkyCable EPG data"
          git push --force
